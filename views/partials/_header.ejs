<% if(user === undefined ){ %>

  <a href="/register"><button>Register</button></a> ||
  <a href="/login"><button>Login</button></a>

<%}else if(user.id){ %>
  <p> <%= user.email %> </p>
  <form method="POST" action="/logout" style="display:inline"><button type="submit" name="Submit"> Log Out </button></form>
<% } %>
