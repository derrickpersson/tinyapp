<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
  <% include ./partials/_header.ejs %>
</head>
<body>
  <ul>
    <% for(let url in urls){ %>
      <li> <%= urls[url] %> || <%= url %> </li>
       <% } %>
  </ul>
</body>
</html>